# Optional — only needed to serve the docs site (mkdocs-material).
#   uv pip install -r requirements-docs.txt
#   mkdocs serve --dev-addr 0.0.0.0:8126     # then browse http://<host>:8126/
mkdocs>=1.6
mkdocs-material
pymdown-extensions
